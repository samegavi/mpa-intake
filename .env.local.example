# Copy to .env.local and fill in your values

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/mpa_intake

# Set to "true" if your DB requires SSL (e.g. hosted on Render/Railway/Supabase)
DATABASE_SSL=false

# Max upload size in bytes (default 10 MB)
MAX_FILE_SIZE=10485760
